"use client";
import styled from "styled-components";

export const LogoTitle = styled.p`
  color: var(--white, #fff);
  font-family: K2D;
  font-size: 28px;
  font-style: normal;
  font-weight: 600;
  line-height: normal;
  letter-spacing: 0.672px;
`;
